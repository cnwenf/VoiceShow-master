<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 实心 -->
    <solid android:color="@android:color/white" />
    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/colorNumber" />
    <!-- 圆角 -->
    <corners android:radius="3dp" />
    <padding
        android:bottom="7dp"
        android:left="50dp"
        android:right="50dp"
        android:top="7dp" />

</shape>